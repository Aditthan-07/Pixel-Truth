version: '3.8'

services:
  web:
    build: .
    container_name: pixeltruth-web
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - HOST=0.0.0.0
      - FLASK_DEBUG=false
    restart: unless-stopped
